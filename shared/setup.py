from setuptools import find_packages, setup

setup(
    name="loyalty_shared",
    version="0.1.0",
    packages=find_packages(),
    description="Shared Pydantic schemas and data models for the Loyalty Program project.",
)
